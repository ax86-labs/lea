version: "2"

run:
  timeout: 5m

linters:
  enable:
    - bodyclose
    - contextcheck
    - errcheck
    - errorlint
    - gosimple
    - govet
    - ineffassign
    - nilerr
    - noctx
    - revive
    - staticcheck
    - unused

issues:
  exclude-use-default: false
